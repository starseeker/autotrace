//
// Created by Nick Winder on 6/10/18.
//

#include "Options.h"

Options::Options(const at_fitting_opts_type &fittingOptions, const at_input_opts_type &inputOptions,
                 const at_output_opts_type &outputOptions) : fittingOptions(fittingOptions),
                                                             inputOptions(inputOptions),
                                                             outputOptions(outputOptions) {

}
